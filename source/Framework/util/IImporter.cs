namespace AnimeKakkoi.Framework.util
{
    public interface IImporter
    {
        //Framework.media.ISource AnalizeSource(object element, Type type);
        //Framework.media.ISource GetSource(string typeName, ref Type rType);
    }
}
